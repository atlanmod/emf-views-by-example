create view copyPizza as

select pizzaMetaModel.Pizza.*,
	   pizzaMetaModel.Pizza join book.Chapter as cookBook,

from 'http://pizza' as pizzaMetaModel,
     'http://book' as book,

 where s.name = t.eContainer().title
  for cookBook